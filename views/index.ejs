<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>After Pose</title>
  <link rel="stylesheet" href="<%= contentUrl %>/build/bundle.css" />
  <link rel="stylesheet" href="//cdn.materialdesignicons.com/5.4.55/css/materialdesignicons.min.css">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Poppins">
  <link rel="icon" type="image/png" href="<%= contentUrl %>/favicon.png" />

  <meta property="og:image" content="<%= url %>">
</head>

<body>
  <div class="grids-container" style="
      --background-color: <%= settings.backgroundColor ? settings.backgroundColor : " #ffffff" %>;
    --background-image: url(<%= backgroundUrl ? backgroundUrl : null %>);
      --decoration-color: <%= settings.decorationColor ? settings.decorationColor : "#000000" %>;
        --decoration-area: <%= settings.decorationArea ? settings.decorationArea : "photo" %>;
          width: 100%;
          height: 100%;
          ">
          <div class="grid-display">
            <div class="grid-decoration"></div>
            <div class="grid-logo">
              <img src="<%= logoUrl ? logoUrl : `${contentUrl}/images/new-assets/After-Pose-Long-Black.png` %>"
                alt="microsite-logo" />
            </div>
            <img class="grid-photo" alt="Your After Pose" src="<%= url %>" />
            <div class="grid-actions">
              <div class="flex-socials">
                <% if (settings.socialFacebook || false) { %>
                  <a title="Facebook" style="color: white; border-color: transparent; background-color: #3b5998;"
                    class="button is-danger is-round is-medium" target="_blank"
                    href="http://www.facebook.com/sharer.php?u=<%= location %>">
                    <span class="icon is-large">
                      <i class="mdi mdi-36px mdi-facebook"></i>
                    </span>
                  </a>
                  <% } %>
                    <% if (settings.socialTwitter || false) { %>
                      <a title="Twitter" style="color: white; border-color: transparent; background-color: #55acee"
                        class="button is-danger is-round is-medium" target="_blank"
                        href="https://twitter.com/intent/tweet?text=<%= settings.socialMessage ? settings.socialMessage : "" %>&url=<%= location %>">
                        <span class="icon is-large">
                          <i class="mdi mdi-36px mdi-twitter"></i>
                        </span>
                      </a>
                      <% } %>
              </div>
              <% if (settings.actionUrl && settings.actionText) { %>
                <div class="flex-socials">
                  <a title="Call to Action"
                    style="color: var(--background-color); border-color: transparent; background-color: var(--decoration-color);"
                    class="button is-danger is-rounded is-medium" target="_blank" href="<%= settings.actionUrl %>">
                    <%= settings.actionText ? settings.actionText : null %>
                  </a>
                </div>
                <% } %>
            </div>
          </div>
  </div>

  <style>
    .grid-display {
      position: relative;
      width: 100%;
      height: 100%;
      background-color: var(--background-color);
      background-image: var(--background-image);
      background-size: contain;
      display: grid;
      row-gap: 1rem;
      justify-items: center;
      align-items: center;
      grid-template-columns: 1fr 2fr 1fr;
      grid-template-rows: 15% 55% 30%;
      grid-template-areas:
        "logo logo logo"
        "photo photo photo"
        ". actions .";
    }

    .grid-decoration {
      grid-area: var(--decoration-area);
      background-color: var(--decoration-color);
      max-height: 25vh;
      width: 100%;
      height: 100%;
    }

    .grid-logo {
      grid-area: logo;
      height: 100%;
      display: flex;
    }

    .grid-logo img {
      width: auto;
      height: 100%;
    }

    .grid-photo {
      grid-area: photo;
      height: 100%;
    }

    .grid-actions {
      grid-area: actions;
      align-self: flex-start;
    }

    .flex-socials {
      display: flex;
      justify-content: center;
      margin-bottom: 1rem;
      gap: 1rem;
    }

    @media screen and (max-width: 1024px) {
      .grid-display {
        grid-template-columns: 0 1fr 0;
      }

      .grid-photo {
        max-height: 100%;
        max-width: 90vw;
        height: auto;
        width: auto;
      }
    }
  </style>

</body>

</html>